# Boat Manager 1.5.1 — release plan

Drafted 19 September 2026, against `main` of the app repo at `46008ca` ("1.5.0")
and `e6d4c25` of this repo.

## The rule for this release

1.5.1 is a point release behind a shipped 1.5.0. It fixes what 1.5.0 broke or left
unfinished, and adds only what is small enough to be verified in a week. No new
entities, no store migration beyond additive optional attributes, no feature that
needs a season of data to prove itself. Everything else is 1.6.

Two lists were merged to build this: the BoatManager Todoist project (28 open
items) and the unfinished build-order steps in `claude/vsc-feature-spec.md` and
`claude/expenses-feature-spec.md`.

---

## Tier 1 — Defects. 1.5.1 does not ship without these.

### 1. Upkeep dashboard misses maintenance log entries — L

Todoist: *Check Hull zincs code — upkeep dashboard not finding the log entries*;
*Link equipID rather than link by name*. These are one bug, not two.

The dashboard matches a logged task to a piece of equipment by **string equality
on names**:

- `Boat Manager 2/Shared/UpkeepStatus.swift:259` — `trackerNames.contains(normalized(task.taskName))`
- `UpkeepStatus.swift:261` — the task's equipment list must *contain* the row's equipment name
- `UpkeepStatus.swift:273` — `maintenanceEquip` is a **comma-separated string of equipment names**, split and lowercased
- `Boat Manager 2/Shared/CoreData/CDFunctions.swift:353-372` — `autoLinkMaintenanceItems` links by `equipType` string equality too

Consequences, all silent: rename a piece of equipment and its history detaches;
"Hull Zincs" never matches an item named "Zincs — Hull"; a comma inside an
equipment name splits it into two phantom entries.

Fix: additive optional `equipID` (UUID) on `MaintenanceTasks`, populated on save
and backfilled once at launch from the existing name match, with the name path
kept as the fallback for legacy rows. Additive and optional, so CloudKit is happy
and no migration is needed.

Ship it with a **Data Health finding** — "this task names equipment that matches
nothing" — which is where an owner will actually see the legacy rows that could
not be backfilled. Data Health shipped in 1.5.0 and is the right home.

### 2. Safety checklist run files an empty log entry — M

Todoist: *Safety checks add empty log entry*. Reported, not yet root-caused.
Start at `Boat Manager 2/AppModels/ChecklistManager.swift` and
`Features/Secondary Features/Maintenance/Checklists/RunChecklistView.swift` — a
run is supposed to file a report in the log, a task per failure and a PDF on the
papers. A 1.5.0 regression in the headline feature of 1.5.0.

### 3. Filter icons incomplete — S

Todoist: *icons for filters (incomplete at the moment)*. Cosmetic, 1.5.0-era,
visible on every list.

---

## Tier 2 — Finish what 1.5.0 started. Safety is half-shipped.

`claude/vsc-feature-spec.md` build order: steps 1–5 and 8 are done, 6 is
cancelled, **7, 9 and 10 are not built**.

### 4. Prefill the safety checklist from what the app already knows — M

Spec step 10, and Todoist *Check safety against boat specs* (boat specs already
carry checkboxes against the USCG list). Prefill from `safetyCategory` on
`Equipment` and `Inventory`, derive the required extinguisher count from
`boatLOA`, warn on expired flares. This is the difference between a checklist
that asks fifteen questions and one that answers half of them first.

### 5. A "Safety Documents" document type — S

Spec step 9. Protected in `DropDownMenuAdmin`, so a scanned ANSC 7012 has a home
and its annual expiry rides the existing Reminders alert. Small, and it closes
the loop the spec deliberately left open by not modelling the examination.

---

## Tier 3 — Small asks that pay their way

### 6. Six-month maintenance interval — S/M

Todoist: *Add 6 months option to maintenance interval*.

`allowedValues: [Int16] = [0, 1, 3, 12, 24]` is declared **three times**:
`Boat Manager 2/AppModels/GlobalVars.swift:434`,
`Features/Secondary Features/Reminders/SubViews/AddReminderToLogBookTaskView.swift:47`,
`.../AddItemToLogBookTaskView.swift:42`. Adding 6 means all three (dedupe to
GlobalVars while there), plus:

- the service-hours mapping in `Features/.../Items/SubViews/SharedSubViews.swift:170-205` needs a `case 6`
- both frequency filter menus need a "Semiannually" toggle (`SharedSubViews.swift:213-260`, `Checklists/SupportingViews/SelectChecklistItem.swift:157-170`) and its predicate
- `frequencyRank` sorting in `Items/ListItems.swift:137`
- **check the segmented picker at phone width** — six segments where there were five

Data side is free: `repeatMonths` already stores any number.

### 7. Invoice/receipt attachment on the Add Expense view — S/M

Todoist: *Add invoice attachment to add expenses view*. `AttachmentsSubView`
already exists and already puts a photo on a task. This is wiring, not new
infrastructure. It is **not** the share-sheet item (item 8) — this is the path
from inside the expense, that one is the path in from outside the app. Build them
together: they write the same cost columns onto the same `Log`.

### 8. Share an invoice/receipt straight to Expenses — S/M

Todoist p1. Filed for 1.5.1, cut on first pass, **restored** — the cut rested on
two claims about the code that are both false:

- *"It needs a share-extension target."* `BoatManagerShare/` already exists and
  ships. It never touches Core Data: it copies the file into the App Group inbox
  and foregrounds the app on `boatmanager://import`
  (`BoatManagerShare/ShareViewController.swift:1-12`). **There is no extension
  work in this at all.**
- *"It needs the normalised expense model first."* It does not. `Documents` already
  has a `log` relationship in the model, and an expense today **is** a `Log` —
  there is no `Expense` entity; `EditExpensesView` edits a selected `Log`
  (`Features/Core Features/Expenses/ExpenseLog/EditExpensesView.swift:17,27-33`)
  and the costs are columns on it.

All the work is at the receiving end, in the app:

- `DocumentDestination` (`Features/Core Features/Documents/InsertDocumentView.swift:22-54`)
  is a six-case enum — boat, newToDo, equipment, maintenanceTask, waypoint, crew.
  Add `.expense`, its label, its icon, `requiresPicker = true`, and a branch in the
  destination section plus its save path.
- **Pick the log, or start one.** The picker resolves a `Log` by date and port,
  and creates one for that date if none exists. 1.5.0 already set this precedent —
  a checklist run on a day with no log starts that log from yesterday's hours and
  position. Reuse it rather than inventing a second rule.
- **Which cost column.** This is the only real design decision, and it is a
  consequence of the wide schema: costFuel, costMarina, costMaintenance,
  costPropane, costPumpout, costWater, costOther. A segmented control over the
  columns that already exist. No schema change, no migration.
- Attach the file through the existing `Documents.log` relationship.

**OCR is separable and out of scope here.** Ship the flow with the fields typed by
hand; a Vision text-recognition prefill is an independent follow-up that changes no
structure. The photo filed against the right log is most of the value, which is
what the expenses spec says about receipt capture anyway.

The one thing that does not survive: this cannot be documented as "file an expense"
in a way the normalised model would later contradict. It files a **document against
a log, with a cost**. That is exactly what the app means by an expense today.

### 9. Equipment PDF report — M

Todoist: *Equipment PDF report that shows everything on the sheet for the piece
of equipment that is open*. Follows `BoatPDFView` / `ListExpensesPDFView`; the
pattern is established, the work is layout.

### 10. Recents at the top of every selectEntity list — M, first to cut

Todoist: *Add recents at the top of any selectEntity list*. Real quality-of-life
gain, but it touches every picker in the app, which is a lot of surface for a
point release. If Tier 1 runs long, this is the item that moves to 1.6.

---

## Tier 4 — Not code. Do these regardless of the release date.

### 11. Merge the App Store copy that is sitting in open branches

- PR #1 (`claude/release-notes-1-5-0-sluwa2`) carries the 1.5.0 App Store copy
  **and** a truncated sentence fix in the Helm help entry, unmerged since 18 Sep.
- `claude/appstore-1-4-3-release-notes-8elp19` (1.4.3 copy) was **never merged**,
  so `appstore/` does not exist on `main`. Both branches add that path.
- Order: merge 1.4.3, rebase #1, merge #1. Otherwise it conflicts.

### 12. help.json gaps for 1.5.0

`help.json` has 105 entries. Searching the whole file: **"Data Health" appears
0 times, "fuel estimate" 0, "share sheet" 0.** Safety has 2 entries, Expenses 2.
Three of 1.5.0's nine What's New items are undocumented. This file publishes to
boatmanager.app by itself, so it is the lowest-risk work on this page. Covers
Todoist *Update BM2 help*.

### 13. UK App Store availability — check today, not at release

Todoist: *Is the App international? Can it be seen on UK AppStore?* This is an
App Store Connect availability setting, not code. If GB is switched off, no
release fixes it and every day it stays off is lost.

### 14. Apple Developer address change document

Todoist item from February, still open. A stale team address can hold a
submission at exactly the wrong moment.

### 15. Delete the duplicated source files — 10 minutes

13 files named `* 2.swift` (Tides, SeasonOutlook, ProjectsView, TideStationPicker,
SafeExternalData, …). **None is referenced in `project.pbxproj`**, and
`ProjectsView 2.swift` is byte-identical to `ProjectsView.swift`. Dead copies
that will eventually be edited by mistake. Part of Todoist *Continue cleaning up
BoatManager refactoring*.

---

## Cut from 1.5.1 — and why

Nothing from the Todoist list is cut on cost grounds. The share-an-invoice item
that this plan first cut is **in the release** — see item 8; the reasoning behind
the cut was wrong on the facts.

**Everything else on the Todoist list is 1.6 or later**, and none of it is a
point-release shape:

| Item | Why not now |
| --- | --- |
| Expenses data model + dashboard rework | Nine-section spec, wide-to-narrow migration. A release of its own. Items 7 and 8 do not depend on it and do not pre-empt it — they write the cost columns that exist today. |
| Voyages/trips; GPS routes to voyages; cruising log with anchorage notes | New entity plus track import, which the expenses spec records as parked. |
| Crew reminders; crew version of the app | New surface, possibly a second target. |
| User-defined fields | Schema and UI generalisation. |
| Connect to Active Captain | Third-party dependency, needs its own spec. |
| Apple Photos album per boat; lean harder on Apple Reminders | Both are integration questions, both need a spec. |
| Paints/solvents/flammables category | It is the `hazardClass` work. `vsc-feature-spec.md` wants it designed knowing VSC items 15b/15c consume it — do it properly in 1.6. |
| Design discussion with Michael Vivona | Input for the next version, not a release item. |

---

## Shape of the release

- **Must ship:** 1, 2, 3, 11, 12
- **Should ship:** 4, 5, 6, 7, 8
- **Ship if Tier 1 lands early:** 9, 10
- **Cut line:** item 10 goes first, then 9.
- **Off the critical path entirely:** 13, 14, 15 — do them this week.

Item 1 is the release's risk. It touches the store, the launch-time repair path
and the dashboard everyone reads. Land it first, with the Data Health finding,
and leave a full week of use on real data before submitting.
